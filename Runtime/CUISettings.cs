namespace CoffeeBean
{
    /// <summary>
    /// UI 模块配置（Editor 生成代码时使用）：
    /// 命名空间（默认 CoffeeBean，业务可改自己的）、生成脚本目录。
    /// </summary>
    public static class CUISettings
    {
        /// <summary>生成的面板脚本默认命名空间。</summary>
        public static string Namespace = "CoffeeBean";

        /// <summary>生成脚本目录（相对 Assets/，如 "Scripts/UI"）。</summary>
        public static string ScriptFolder = "Scripts/UI";
    }
}
